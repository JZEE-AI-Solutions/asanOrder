BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[forms] ADD [formCategory] NVARCHAR(1000) NOT NULL CONSTRAINT [forms_formCategory_df] DEFAULT 'SIMPLE_CART';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
